apiVersion: planning/v1
kind: UserStories
metadata:
  name: epic-10
  lastUpdated: "2026-05-19"
spec:
  items:
    - id: US-10.1
      title: Trigger parallel council review with 3 AI agents
      status: done
      description: Initiate a council correct review that runs 3 AI agents in parallel.
      acceptanceCriteria: ["Council review triggers 3 parallel agents"]
      evidence: ['Verified implemented']
      priority: P0
      estimate: M
      workType: build
      linkedRequirements: [FR-05]
      epicId: epic-10
    - id: US-10.2
      title: Each agent reviews prompt from unique perspective
      status: done
      description: Each council agent evaluates the prompt from a distinct, configured perspective.
      acceptanceCriteria: ["Each agent provides unique perspective feedback"]
      evidence: ['Verified implemented']
      priority: P0
      estimate: S
      workType: build
      linkedRequirements: [FR-05]
      epicId: epic-10
    - id: US-10.3
      title: Agents use configured system prompts and models
      status: done
      description: Council agents load and use their configured system prompts and model settings.
      acceptanceCriteria: ["Agents use correct system prompts and models"]
      evidence: ['Verified implemented']
      priority: P0
      estimate: M
      workType: build
      linkedRequirements: [FR-05, FR-04]
      epicId: epic-10
    - id: US-10.4
      title: Override agent prompts per-session
      status: done
      description: Allow temporary override of agent system prompts for a single council session.
      acceptanceCriteria: ["Prompt overrides apply for the session only"]
      evidence: ['Verified implemented']
      priority: P0
      estimate: S
      workType: build
      linkedRequirements: [FR-05]
      epicId: epic-10
    - id: US-10.5
      title: Stream council results in real-time via SSE
      status: done
      description: Stream council agent responses to the client in real-time using SSE.
      acceptanceCriteria: ["Council results stream in real-time"]
      evidence: ['Verified implemented']
      priority: P0
      estimate: M
      workType: build
      linkedRequirements: [FR-05, NFR-01]
      epicId: epic-10
    - id: US-10.6
      title: Display all agent feedback with clear agent identification
      status: done
      description: Present all council agent feedback with clear labeling of which agent produced each response.
      acceptanceCriteria: ["Each feedback item shows agent identity"]
      evidence: ['Verified implemented']
      priority: P0
      estimate: S
      workType: build
      linkedRequirements: [FR-05]
      epicId: epic-10
